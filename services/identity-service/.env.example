# Identity Service Environment Variables

# Server
PORT=3001
NODE_ENV=development

# Database (PostgreSQL - matches docker-compose.yml)
DATABASE_URL=postgresql://gymato:gymato_dev@localhost:5432/gymato

# JWT
JWT_SECRET=this-is-a-super-secret-key-at-least-32-characters-long
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=30

# bcrypt (Bun uses its native password API)
BCRYPT_ROUNDS=12

# Service
SERVICE_NAME=identity-service
